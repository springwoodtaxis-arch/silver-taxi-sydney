#!/usr/bin/env node
/* Retired: public Google profile/map exposure was intentionally removed.
   Use repository grep/build validation for the safe direct-booking SEO panels. */
console.log('Public GBP/map validation retired: safe direct-booking SEO panels are now required.');
